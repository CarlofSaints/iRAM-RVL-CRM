Fix Send DN bug + add delivery page status debugging

- Send DN button now calls dedicated /api/pick-slips/send-dn route
  (was reusing pick slip send which sent pick slip PDF instead of DN)
- New send-dn route re-generates delivery note PDF from slip data
- Separate Send DN modal with rep dropdown and email fields
- Receipt/complete endpoint now guards against non-booked slips
  (prevents accidental overwrite of in-transit/delivered status)
- Delivery page shows actual status when "Not Available" for debugging
- StatusBadge tooltip text wrapping fix (whitespace-normal)

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
