Fix: avatar proxy 401 — drop requireLogin from image route

<img src=...> tags can't attach custom headers, so requireLogin()
(which checks x-user-id) rejected every browser avatar request and
rendered as a broken image / alt text.

Removed the guard — accessing an avatar still requires knowing the
user ID. Same trust model as GitHub/Slack avatars.
